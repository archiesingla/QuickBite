import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  FlatList,
  Image,
  ScrollView,
} from "react-native";
export default function OrderHistory(){
    return(
        <Text> From OrderHistory </Text>

    );
}